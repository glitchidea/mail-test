{
  "name": "email-sender-worker",
  "version": "1.0.0",
  "description": "Email sender Cloudflare Worker",
  "main": "src/worker.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "emailjs": "^4.0.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}